<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F4F6F8"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Header tối giản -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/bg_icon_circle"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            android:src="@drawable/outline_arrow_back_24"
            app:tint="#2E3148" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Lịch sử"
            android:textColor="#2E3148"
            android:textSize="18sp"
            android:textStyle="bold" />
    </FrameLayout>

    <!-- Card kết quả gọn gàng -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Điểm số nổi bật bên trái -->
            <TextView
                android:id="@+id/txtHistoryScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="--"
                android:textColor="#22A06B"
                android:textSize="32sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/unitScore"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toEndOf="@id/txtHistoryScore"
                android:layout_alignBaseline="@id/txtHistoryScore"
                android:layout_marginStart="4dp"
                android:text="/24"
                android:textColor="#7B8194"
                android:textSize="14sp" />

            <!-- Mức độ nằm bên phải điểm -->
            <TextView
                android:id="@+id/txtHistoryLevel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:background="#F0F9F4"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:text="Mức độ"
                android:textColor="#22A06B"
                android:textSize="14sp"
                android:textStyle="bold" />

            <!-- Ngày tháng nằm dưới cùng -->
            <TextView
                android:id="@+id/txtHistoryDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/txtHistoryScore"
                android:layout_marginTop="8dp"
                android:text="Ngày test: --"
                android:textColor="#7B8194"
                android:textSize="12sp" />

        </RelativeLayout>
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/txtNoData"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:gravity="center"
        android:text="Không có dữ liệu lịch sử"
        android:textColor="#7B8194"
        android:textSize="14sp"
        android:visibility="gone" />

</LinearLayout>