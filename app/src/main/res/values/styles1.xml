<resources>

    <style name="style1">
        <item name="android:layout_width">30dp</item>   <!-- nhỏ lại -->
        <item name="android:layout_height">45dp</item>

        <item name="android:layout_margin">4dp</item>  <!-- giảm khoảng cách -->

        <item name="android:gravity">center</item>
        <item name="android:textSize">14sp</item>      <!-- chữ nhỏ lại -->
        <item name="android:textStyle">bold</item>

        <item name="android:inputType">number</item>
        <item name="android:maxLength">1</item>

        <item name="android:background">@drawable/input_square</item>
        <item name="android:textColor">#333333</item>
    </style>

</resources>